<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginStart="8dp"
    android:layout_marginTop="4dp"
    android:layout_marginEnd="8dp"
    android:layout_marginBottom="4dp">

    <android.support.v7.widget.CardView
        android:id="@+id/ii_cv_holder"
        android:layout_width="match_parent"
        android:layout_height="@dimen/imageSizeHeight"
        android:elevation="@dimen/cardCornerRadius"
        app:cardCornerRadius="@dimen/cardCornerRadius"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">
        <!--ii - > Item Image-->
        <!--iv - > Image View-->
        <ImageView
            android:id="@+id/ii_iv_image"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop" />
    </android.support.v7.widget.CardView>

    <TextView
        android:id="@+id/ii_tv_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:layout_marginBottom="4dp"
        android:text="@string/app_name"
        android:textColor="@color/titleTextColor"
        android:fontFamily="@font/source_sans_pro_semibold"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="@id/ii_cv_holder"
        app:layout_constraintStart_toStartOf="@id/ii_cv_holder"
        app:layout_constraintTop_toBottomOf="@id/ii_cv_holder" />
</android.support.constraint.ConstraintLayout>